import 'package:flutter/cupertino.dart';

class HomeScreenNotesTab extends StatelessWidget {
  const HomeScreenNotesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notes coming soon!'),
    );
  }
}
